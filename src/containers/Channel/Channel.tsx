import MainNavbar from '../../components/InnerNavbar';

interface ChannelProps {}
function Channel(props: ChannelProps) {
  return (
    <>
      <MainNavbar />
      <div>asdadas</div>
    </>
  );
}

export default Channel;
